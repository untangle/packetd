version: '2'
services:
    musl:
      image: untangleinc/packetd:build-musl
      volumes:
        - ${HOME}/go/src:/go/src
    glibc:
      image: untangleinc/packetd:build-glibc
      volumes:
        - ${HOME}/go/src:/go/src
